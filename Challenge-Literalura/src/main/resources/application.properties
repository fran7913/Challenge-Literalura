spring.application.name=Challenge-Literalura
# Conexión a la base de datos
spring.datasource.url=jdbc:postgresql://${DB_HOST}:5432/challenge_literalura
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver
# Configuración de Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# Muestra en la consola todas las consultas SQL que Hibernate ejecuta (como SELECT, INSERT, UPDATE,

# Hace que el SQL que aparece en la consola se vea más legible y ordenado (con saltos de línea e indentación).
spring.jpa.format-sql=true  


# Desactiva el log de SQL de Hibernate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

